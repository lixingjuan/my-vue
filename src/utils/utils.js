/**
 * @des 颜色转换，十六进制 => RGB
 * @param {String} 16进制颜色
 * @return:
 */
const colorHexToRgb = function() {};

/**
 * @des 颜色转换，RGB => 十六进制
 * @param {String} rgb颜色嗯
 * @return:
 */
const colorRgbToHex = function() {};

export { colorHexToRgb, colorRgbToHex };
